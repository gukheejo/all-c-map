# All-C-Map Agent Guide

이 문서는 저장소 전체에 적용된다. 작업 전 이 문서와 현재 코드, `git status`를 확인한다.

## 프로젝트와 핵심 흐름

- 모바일 우선 React 18 + Vite 5 데모이며 백엔드, 인증, 데이터베이스, 영구 저장소는 없다.
- 최대 콘텐츠 폭은 `480px`이고 모바일 안전영역과 문서 스크롤을 지원해야 한다.
- 초기 화면은 `2. 올영매장`이다. 공통 하단 탭은 `올영매장`만 활성화하며 다른 탭은 비활성 상태다.
- 주요 흐름은 `2 → 3-b → 4/3.5 → product → pickup-sheet → 5 → 6 → 7`이다. 제거된 `4-b` 확인 팝업은 다시 추가하지 않는다.

## 디자인과 Figma MCP

- 기준은 Figma 파일 `00TMv732FpJjRSJPtsrYCz`의 FINAL 프레임이다. 주요 노드는 올영매장 메인 `856:2731`, 3-d 확장 지도 시트 `1334:2728`, 상품 상세/픽업 흐름 `1392:5432`다.
- 디자인 구현 전 대상 노드에 Figma MCP `get_design_context`를 호출한다. Figma URL에서 file key와 node ID를 정확히 추출하고, 노드 ID를 추측하지 않는다.
- 반환된 React/Tailwind는 구조·치수 참고용이다. 기존 React 컴포넌트, 일반 CSS, 디자인 토큰과 에셋을 우선 재사용한다.
- 첨부 화면은 보조 자료다. 최신 Figma와 충돌하면 사용자에게 확인한다. MCP 오류는 권한과 node ID를 확인하고, 타임아웃이면 더 작은 하위 노드로 재시도한다.
- 만료 가능한 Figma 에셋 URL을 소스에 남기지 말고 필요한 파일은 `public/`에 보관한다.
- 단일 화면 크기에 맞춘 좌표 하드코딩 대신 `dvh`, 퍼센트, 실측 높이와 기존 반응형 계산을 사용한다.

## 지도와 모바일 UI

- 지도는 카카오 Maps JavaScript SDK와 `src/kakao-map.js`를 사용한다. 고정 위치는 `src/store-utils.js`의 `FIXED_LOCATION`이 단일 기준이다.
- 최초 올클맵 진입 시 매장을 자동 선택하지 않고 충무로 주변 최대 10개 매장을 표시한다. 올영매장 미니 지도도 카카오 SDK 기반이다.
- 선택 마커는 가장 앞에 표시하며 얇은 초록 테두리와 그림자를 유지한다. 선택 매장명은 바텀시트 위 중앙에 보이도록 기존 오프셋 계산을 사용한다.
- `public/icons/map-dim-screen.svg` dim은 지도에서 항상 유지한다. 선택 매장이나 크루톡 시트를 닫을 때 제거하지 않는다.
- 지도 배경을 누르면 열린 바텀시트를 닫고 검색 버튼을 복원한다. `영업중`은 눌림 상태만 토글하며 목록은 바꾸지 않는다.
- 매장 상품·크루톡 시트는 기본 높이 `55%`이고 탭·드래그로 collapsed/expanded 상태를 전환한다. 시트가 열리면 공통 하단 내비게이션을 숨긴다.
- 일반 화면은 문서 스크롤을, 지도 화면은 고정 뷰포트 내부 스크롤을 사용한다. 상품 상세에서는 공통 하단 내비게이션 대신 픽업 CTA를 표시한다.

## 상품과 매장 데이터

- `src/data.js`의 상품 15개를 사용한다. 외부 올리브영 CDN 썸네일은 네트워크가 없으면 표시되지 않을 수 있다.
- 매장 한정 상품은 `EXCLUSIVE_PRODUCTS`에 두고 정규 15종과 분리한다. 매장의 `exclusiveProductIds`에 적힌 상품만 `resolveStoreCatalog`가 해당 매장 목록 맨 앞에 고정하며, 조건 뱃지 대신 상품이 가진 값을 그대로 쓴다.
- 마커 숫자는 1~15이며 선택 매장의 중복 없는 상품 행 개수와 일치해야 한다. 지도와 매장 상세는 같은 `buildStoreProducts` 결과를 사용한다.
- `AI PICK`은 매장별 최대 3개이며 목록부터 상품 상세·픽업까지 유지한다.
- 상태 뱃지는 없음·`유통기한`·`패키지 파손`을 매장 ID 기반의 안정적인 규칙으로 자연스럽게 분배한다.
- 구매 수량은 1 이상, 잔여 재고 이하로 제한한다. 가격은 원가보다 최소 35% 할인된 정적 데모 값이다.

## 크루톡과 매장 소식

- 올클맵 크루톡은 주변 10개 지점의 글을 모으고, 매장 소식은 선택 지점 데이터만 보여 준다.
- `최신순`은 날짜 내림차순, `등록순`은 오름차순이며 두 화면에서 같은 필터 스타일을 사용한다.
- 상품명, 옵션명, 매장명, 본문을 검색한다. 공지와 크루톡 검색어 상태는 독립적이다.
- 크루톡 본문은 기본 2줄 말줄임이고 탭할 때 전체 표시와 접힘을 전환한다. 매장별 개수·날짜·문구는 서로 다르게 유지한다.
- 지도 시트의 입고알림은 해당 매장의 공지 탭으로, 크루톡 상품은 상품 상세로 이동한다.

## 코드와 상태 관리

- `src/pickup-flow.js`는 화면, 선택 매장·상품, 장바구니와 단일 `overlay` 상태 전이의 기준이다.
- 주요 UI는 `src/components/MapScreen.jsx`, `Screens.jsx`, `Overlays.jsx`에 있고, 데이터와 순수 로직은 `src/data.js`, `src/store-utils.js`에 있다.
- 화면 번호 문자열은 Figma 흐름과 reducer 계약에 사용된다. 라우터 도입이나 일괄 이름 변경 전에는 상태 전이 테스트와 사용자 승인이 필요하다.
- 클릭 UI는 `button`과 명확한 accessible name을 사용하고, 기존 에셋은 `public/icons`, `public/photos`에서 우선 재사용한다.

## 환경과 보안

```bash
npm install
cp .env.example .env.local
npm run dev
```

- `.env.local`의 `VITE_KAKAO_MAP_KEY`와 모든 키·토큰·로그인 정보는 커밋하지 않는다. `VITE_` 값은 브라우저에 노출되므로 카카오 콘솔의 JavaScript SDK 도메인 제한을 사용한다.
- 실제 Vite 포트를 카카오 콘솔에 등록한다. 배포는 `https://all-c-map.vercel.app`을 등록하고 Vercel 환경변수 변경 후 재배포한다.
- 지도 오류는 환경변수, 카카오맵 제품 활성화, 등록 도메인, 브라우저 콘솔 순으로 확인한다.
- 데모 범위를 런타임 스크래핑, 백엔드, 결제나 실시간 재고 연동으로 임의 확장하지 않는다.

## 검증과 Git

```bash
npm test
npm run build
git diff --check
```

- 시각적 동작 변경은 브라우저에서 직접 확인한다. 최소 흐름은 `올영매장 → 올클맵 → 마커/시트 → 매장·상품 상세 → 픽업 → 장바구니 → 바코드`다.
- 좁은 모바일 폭과 `480px`에서 위치, 겹침, 말줄임과 안전영역을 확인한다.
- 새 작업을 시작할 때마다 먼저 사용자 변경을 확인하고 안전하게 보존한 뒤, `main`으로 이동해 `git pull --ff-only`로 원격 `main`을 최신 상태로 갱신한다. pull이 실패하면 오래된 로컬 `main`에서 작업을 시작하지 말고 원인을 확인하거나 사용자에게 알린다.
- 새 `feat/...` 또는 `fix/...` 브랜치는 반드시 위에서 갱신한 최신 `main`에서 만든다. `main`에 직접 커밋하지 않는다.
- 커밋·push·PR·merge는 사용자 요청 범위에서만 수행한다. PR은 기본적으로 `main` 대상이며, 기존 열린 PR이 있으면 중복 생성하지 않고 같은 브랜치에서 갱신한다.
- `git reset --hard`, 무단 force push와 관련 없는 파일 되돌리기를 하지 않는다. 스쿼시 머지는 명시적으로 요청받았을 때만 수행한다.
