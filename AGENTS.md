# All-C-Map Agent Guide

이 문서는 저장소 전체에 적용된다. 이 프로젝트를 수정하는 에이전트는 작업 전에 이 문서와 현재 코드, `git status`를 먼저 확인한다.

## 프로젝트 성격

- 올리브영 매장 탐색과 클리어런스 상품 픽업 흐름을 보여 주는 모바일 우선 React 데모다.
- React 18 + Vite 5 기반이며 별도 백엔드, 인증, 데이터베이스, 영구 저장소는 없다. 새로고침하면 앱 상태는 초기화된다.
- 최대 콘텐츠 폭은 `480px`이다. 모바일 브라우저에서 문서 스크롤과 안전영역(`env(safe-area-inset-bottom)`)이 정상 동작해야 한다.
- 초기 진입 화면은 `2. 올영매장`이다. 공통 하단 탭에서는 `올영매장`만 활성화하고 나머지 탭은 비활성 상태를 유지한다.
- 주요 데모 흐름은 `2 → 3-b → 4/3.5 → product → pickup-sheet → 5 → 6 → 7`이다. 제거된 `4-b` 확인 팝업을 다시 추가하지 않는다.

## 디자인 기준

- 원본 디자인은 Figma 파일 `00TMv732FpJjRSJPtsrYCz`의 FINAL 프레임이다.
- 자주 참조한 노드는 올영매장 메인 `856:2731`, 3-d 확장 지도 시트 `1334:2728`, 상품 상세/픽업 흐름 `1392:5432`이며, 지도 상태는 `3-b`, `3-c`, `3-d` 프레임을 함께 확인한다.
- Figma 접근이 가능하면 디자인 구현 전에 Figma MCP로 해당 노드의 최신 상태를 확인한다. 첨부 스크린샷은 보조 자료이며 최신 Figma와 충돌하면 사용자에게 확인한다.
- 픽셀 값을 화면 크기 하나에만 맞춰 하드코딩하지 않는다. `dvh`, 퍼센트, 실측 높이와 기존 반응형 계산 함수를 우선 사용한다.
- `docs/superpowers/` 아래 과거 문서에는 MapLibre와 이전 상품 반복 방식이 남아 있을 수 있다. 현재 카카오맵 구현과 이 문서가 최신 기준이다.

### Figma MCP 사용 규칙

- 디자인을 코드로 옮길 때는 대상 노드에 대해 Figma MCP의 `get_design_context`를 먼저 호출한다. `get_metadata`나 스크린샷만으로 이를 대신하지 않는다.
- Figma URL의 `/design/{fileKey}/...`와 `node-id`를 각각 정확한 file key와 node ID로 변환한다. 노드 ID가 없는 URL은 임의로 추측하지 않는다.
- `get_design_context`가 반환하는 React/Tailwind 코드는 구조·치수·타이포그래피를 파악하기 위한 참고 자료다. 그대로 붙여 넣지 말고 이 저장소의 React 컴포넌트와 일반 CSS 방식에 맞춘다.
- 기존 컴포넌트, CSS 변수, 아이콘과 이미지 에셋을 먼저 확인해 재사용한다. Figma 에셋 URL은 만료될 수 있으므로 커밋할 에셋은 내려받아 `public/` 아래에 보관한다.
- 응답 힌트는 Code Connect, 컴포넌트 문서, 디자인 주석, 디자인 토큰, 원시 치수 순으로 우선한다. 최종 시각 판단에는 함께 반환된 스크린샷을 사용한다.
- MCP 호출이 실패하면 오류를 읽고 권한·node ID를 확인한다. 타임아웃이면 더 작은 하위 노드로 재시도하며, 연결 가능한 상태에서 스크린샷만 보고 임의 구현으로 우회하지 않는다.

## 지도와 위치

- 지도 SDK는 MapLibre가 아니라 카카오 Maps JavaScript SDK다. 지도 로더는 `src/kakao-map.js`를 재사용한다.
- 고정 사용자 위치는 `서울시 중구 필동로 26 (필동2가 101-1)`, 좌표는 `src/store-utils.js`의 `FIXED_LOCATION`이 단일 기준이다.
- 지도 최초 진입 시 특정 매장을 자동 선택하지 않는다. 충무로 주변 최대 10개 매장을 보여 주고, 초기 확대 수준은 현재의 카카오 지도 레벨 제한을 유지한다.
- 올영매장 메인의 미니 지도와 올클맵 본 지도 모두 카카오 SDK 기반이어야 한다.
- 선택 마커는 다른 마커보다 앞에 표시하고, 얇은 초록색 테두리와 그림자를 유지한다. 선택 매장명은 바텀시트 위 가로 중앙에 오도록 지도 카메라를 이동한다.
- 선택 마커 위치는 `getSelectedMarkerOffset`과 `getKakaoSelectedPanOffset`으로 계산한다. 현재 바텀시트와의 기준 간격은 `SELECTED_MARKER_GAP`이며 고정 화면 좌표로 대체하지 않는다.
- 지도용 dim 이미지는 `public/icons/map-dim-screen.svg`이며 의도적으로 항상 유지된다. 크루톡이나 매장 바텀시트를 닫을 때 함께 사라지게 하지 않는다.
- 지도 배경을 누르면 선택 매장/크루톡 바텀시트가 닫히고 필요한 검색 버튼이 돌아온다.
- `영업중` 필터는 눌림 상태만 토글하며 현재 데모에서는 결과 목록을 필터링하지 않는다.

## 바텀시트와 모바일 동작

- 매장 상품 및 크루톡 바텀시트의 기본 collapsed 높이는 화면의 `55%`다.
- 핸들 탭과 위아래 드래그를 모두 지원하고, 거리 또는 속도 임계값을 넘으면 빠르게 collapsed/expanded 상태로 스냅한다.
- 바텀시트가 열리면 공통 하단 내비게이션을 화면 아래로 숨긴다. 상품 상세에서는 공통 하단 내비게이션을 표시하지 않고 픽업 CTA를 하단에 둔다.
- 일반 콘텐츠 화면은 문서 스크롤을 사용해 모바일 브라우저 바가 자연스럽게 접힐 수 있어야 한다. 지도 화면만 고정 뷰포트 내부 스크롤 구조를 유지한다.
- 픽업 시트, 토스트, 바코드는 동시에 중첩되지 않도록 `overlay` 단일 상태를 사용한다.

## 상품과 매장 데이터 규칙

- 데모 카탈로그는 `src/data.js`의 실제 상품 15개를 사용한다. 상품 썸네일은 올리브영 CDN 외부 URL이므로 네트워크가 없으면 표시되지 않을 수 있다.
- 매장 마커의 재고 숫자는 1~15 범위이며, 선택한 매장의 상품 행 개수와 같아야 한다. 한 매장 목록 안에서 상품 ID를 중복시키지 않는다.
- 매장별 상품 배열은 반드시 `buildStoreProducts(PRODUCTS, store.stock, 3, store.id)`로 만든다. 지도 목록과 매장 상세가 별도 하드코딩 배열을 갖지 않게 한다.
- `AI PICK`은 한 매장 목록에서 최대 3개만 표시한다. 목록, 매장 상세, 상품 상세, 픽업 팝업으로 이동해도 같은 상품의 AI PICK 상태를 유지한다.
- 상태 뱃지는 `뱃지 없음`, `유통기한`, `패키지 파손`을 매장별로 균형 있게 배치한다. 단순 반복 순서가 아니라 매장 ID 기반의 안정적인 셔플을 사용하므로 새로고침 후에도 결과가 같아야 한다.
- 상품 수량은 최소 1이고 해당 상품의 잔여 재고를 초과할 수 없다. 장바구니에서도 같은 상한을 유지한다.
- 데모 가격은 원가 대비 최소 35% 할인된 값이며, 실제 실시간 가격·재고라고 표현하지 않는다.

## 크루톡과 매장 소식

- 올클맵의 크루톡은 주변 10개 지점의 글을 모아 보여 준다. 매장 상세의 매장 소식은 선택 지점 데이터만 보여 준다.
- 크루톡 바텀시트는 처음에 절반가량 열린 상태로 표시되고 위로 스와이프하면 확장된다.
- `최신순`은 날짜 내림차순, `등록순`은 날짜 오름차순이다. 지도 크루톡과 매장 소식에서 같은 콤팩트 화살표 스타일을 사용한다.
- 검색은 상품명, 옵션명, 매장명, 본문을 대상으로 하며 공지와 크루톡 검색어 상태는 서로 독립적이다.
- 모든 크루톡 본문은 기본 2줄 말줄임이며 누르면 전체 표시, 다시 누르면 접힘 상태로 돌아간다.
- 매장별 공지와 크루톡은 개수, 날짜, 문구가 완전히 같지 않도록 현재의 안정적인 매장별 생성 규칙을 유지한다.
- 지도 매장 시트의 입고알림을 누르면 해당 지점의 매장 소식 중 공지 탭으로 이동한다. 크루톡 상품을 누르면 해당 상품 상세로 이동한다.

## 상태와 주요 파일

- `src/App.jsx`: 화면 조합, 선택 매장 기반 상품 해석, 오버레이 연결.
- `src/pickup-flow.js`: 화면·선택 매장·상품·장바구니·오버레이 상태 전이의 단일 기준.
- `src/components/MapScreen.jsx`: 카카오 지도, 마커, dim, 지도/크루톡 바텀시트와 제스처.
- `src/components/Screens.jsx`: 올영매장, 상품 상세, 매장 상세/소식, 장바구니 화면.
- `src/components/Overlays.jsx`: 픽업 주문 시트와 바코드 오버레이.
- `src/store-utils.js`: 거리, 정렬, 검색, 매장별 상품·뱃지 생성 등 순수 로직.
- `src/data.js`: 매장, 상품, 공지, 크루톡 데모 데이터.
- `src/styles.css`: 모바일 레이아웃과 디자인 토큰. 기존 선택자 범위를 확인하고 전역 스타일 충돌을 피한다.
- `test/ui-contract.test.mjs`: 화면 계약과 순수 로직의 회귀 테스트. 동작 변경은 가능한 한 실패 테스트부터 추가한다.

화면 번호 문자열은 Figma 흐름과 기존 reducer 계약 때문에 사용된다. 별도 라우터를 도입하거나 번호를 의미 기반 이름으로 일괄 변경하지 말고, 필요하면 먼저 상태 전이 테스트와 사용자 승인을 확보한다.

## 로컬 실행과 환경변수

```bash
npm install
cp .env.example .env.local
npm run dev
```

- `.env.local`에는 `VITE_KAKAO_MAP_KEY`를 설정한다. Vite가 출력한 실제 포트를 확인하며 `5173` 또는 `5174`라고 가정하지 않는다.
- `VITE_` 환경변수는 브라우저 번들에 노출된다. 실제 키를 소스, 문서, 테스트, PR 본문에 기록하지 말고 카카오 개발자 콘솔의 JavaScript SDK 도메인 제한으로 보호한다.
- 로컬 도메인은 실제 실행 주소와 포트가 일치해야 한다. 배포 환경은 기본적으로 `https://all-c-map.vercel.app`을 카카오 JavaScript SDK 도메인에 등록한다.
- Vercel에는 `VITE_KAKAO_MAP_KEY`를 환경변수로 추가하고 변경 후 재배포한다. 고유 Preview URL을 사용할 경우 그 도메인도 카카오 콘솔에 등록해야 하므로 시연에는 고정 Production URL을 우선한다.
- 지도 로드 실패 시 키 존재 여부, 카카오맵 제품 활성화, 등록 도메인, 브라우저 콘솔 오류 순서로 확인한다.

## 검증 기준

모든 코드 변경 후 아래 명령을 실행한다.

```bash
npm test
npm run build
git diff --check
```

- 지도·바텀시트·모바일 스크롤처럼 시각적 상호작용이 바뀌면 실행 중인 브라우저에서도 직접 확인한다.
- 최소 확인 흐름: 최초 올영매장 → 올클맵 미선택 상태 → 마커 선택 → 시트 드래그/배경 닫기 → 매장 상세 → 상품 상세 → 픽업 수량 → 장바구니 → 바코드.
- 반응형 변경은 좁은 모바일 폭과 `480px` 캔버스 모두 확인한다.
- 테스트 통과만으로 Figma 일치를 단정하지 않는다. 위치·겹침·말줄임·안전영역은 실제 렌더링을 함께 확인한다.

## Git과 PR 작업 방식

- 작업 시작 시 `git status --short --branch`로 사용자 변경을 확인하고 보존한다.
- 새 작업은 최신 `main`을 pull한 뒤 별도 `feat/...` 또는 `fix/...` 브랜치에서 진행한다. `main`에 직접 커밋하지 않는다.
- 충돌 확인이나 `main` 반영이 필요하면 현재 브랜치의 사용자 변경을 잃지 않는 방식으로 처리한다. `git reset --hard`, 무단 force push, unrelated 파일 되돌리기를 하지 않는다.
- 커밋·push·PR 생성은 사용자가 요청했을 때 수행한다. PR 대상은 기본적으로 `main`이며 변경사항과 검증 결과를 짧게 적는다.
- PR을 만들었다고 자동으로 머지하지 않는다. 머지는 사용자의 명시적 요청이 있을 때만 하며, 스쿼시 머지를 요청하면 GitHub의 squash 방식으로 처리한다.
- PR 후 수정은 같은 브랜치에 추가 커밋하고 기존 PR을 갱신한다. 중복 PR을 만들기 전에 현재 브랜치의 열린 PR을 확인한다.

## 변경 시 주의사항

- 사용자에게 받은 API 키, 토큰, 로그인 정보는 저장소에 남기지 않는다. `.env.local`은 커밋하지 않는다.
- 외부 올리브영 페이지를 런타임에 스크래핑하는 기능은 없다. 현재 상품 데이터는 데모용 정적 데이터다.
- 기존 아이콘과 Figma 에셋은 `public/icons`와 `public/photos`에서 우선 재사용한다. 같은 용도의 임의 이모지나 시스템 아이콘으로 교체하지 않는다.
- 클릭 가능한 UI는 `button`과 명확한 accessible name을 유지한다. 보이는 텍스트만 있는 `div` 클릭 영역을 새로 만들지 않는다.
- 기능을 추가하면서 현재 데모 범위를 백엔드, 결제, 실시간 재고 연동으로 임의 확장하지 않는다.
